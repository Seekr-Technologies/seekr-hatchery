# Task: unify

**Status**: complete
**Branch**: unify
**Created**: 2026-03-06 08:15

## Objective

Evaluate changes copied from the private repo, clean up, and split into logical commits. Scope limited to bringing in existing functionality only; fix-dind-2 will be rebased onto the result in a future task.

## Summary

### Changes kept (3 logical commits)

1. **fix: handle FileNotFoundError in docker/podman availability checks** (`cc90ba1`)
   - `docker_available()` and `podman_available()` now catch `FileNotFoundError` when docker/podman binaries aren't on PATH, instead of crashing.

2. **fix: use find_task_file in session_prompt for cross-day resume** (`4f6d8b5`)
   - `session_prompt()` now uses `find_task_file()` (glob match by name) instead of `task_file_name()` (constructs path using today's date). Fixes resuming tasks created on a different day.

3. **feat: refactor DinD Dockerfile into shared constant + add missing caps** (`4496e63`)
   - Moved the Podman-in-Podman Dockerfile section from hardcoded commented-out lines in `Dockerfile.template` into a `DIND_DOCKERFILE_LINES` constant in `docker.py`. Template now uses `{{DIND}}` placeholder. Added `_comment_out()` helper.
   - Added `AUDIT_WRITE`, `FSETID`, `KILL`, `NET_BIND_SERVICE`, `NET_RAW` to DinD caps (OCI defaults needed for crun capset during `podman build` RUN steps).
   - Replaced the skipped DinD test placeholder with full integration tests (podman info/pull/run/build inside nested container).

### Changes discarded

- **`ensure_tasks_dir` rename to `ensure_claude_tasks_dir`**: Broken — `cli.py` and tests still call `ensure_tasks_dir`. Also misleading (creates `.hatchery/`, not `.claude/`).
- **`.hatchery/seccomp.json`**: Exact duplicate of `src/seekr_hatchery/resources/seccomp.json`.
- **Comment/docstring tweaks**: Minor wording changes making docs Claude-specific, but the tool supports Codex too. Not worth the diff noise.

### Future work

- Rebase `hatchery/fix-dind-2` onto this branch to bring in `cap_add` config field, `_VALID_CAPS` validation, and `NET_ADMIN`/`NET_RAW` additions.
